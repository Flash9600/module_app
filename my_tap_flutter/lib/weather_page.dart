import 'package:flutter/cupertino.dart';

class WeatherPage extends StatelessWidget {
  WeatherPage();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Hello, my tap weather'),
    );
  }
}